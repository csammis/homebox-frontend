[project]
name = "homebox-frontend"
version = "0.1.0"
description = "A Flask server proxy for requesting items stored in HomeBox for display in a for-sale gallery format"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=23.0.0",
    "requests>=2.32.5",
]
